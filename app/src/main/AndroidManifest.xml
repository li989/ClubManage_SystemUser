<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"

        package="com.example.tst1">

        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.CAMERA" />
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
        <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

        <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
            <activity android:name=".announce_over.School_Notice_List"></activity>
            <activity android:name=".announce_over.School_notice_item" />
            <activity android:name=".award.award_info" />
            <activity android:name=".award.award" />
            <activity android:name=".activity.activity" />
            <activity android:name=".activity.activity_info" />
            <activity android:name=".announce.announce" />
            <activity android:name=".club_add.club_audit_item" />
            <activity android:name=".club_add.club_audit_list" />
            <activity android:name=".welcome.login" />
            <activity android:name=".welcome.welcome">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />

                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>
            <activity android:name=".welcome.register" />
            <activity android:name=".MainActivity"
                android:exported="true"/>
        </application>

    </manifest>